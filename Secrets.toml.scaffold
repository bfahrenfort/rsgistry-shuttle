# Shuttle will use `Secrets.toml` as the secrets during production
# Likewise, `Secrets.dev.toml` is used during development if available
# DO NOT TRACK EITHER OF THESE FILES WITH VERSION CONTROL
PASSWORD = "Password_To_Your_Postgres_Database" # recommend using `pwgen 64 1 -s`
ROOT_ADMIN = # Same recommendation, file is designed to error out if this is not set since it's so dangerous 
